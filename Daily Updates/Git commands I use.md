- Check a running process: `ps aux | grep nvim`
- Kill a running process: `kill PID`
- Force kill a running process: `kill -9 PID`

What is a swap file? This is a virtual memory that uses the hard drive or SSD space to store inactive data, while freeing up the physical RAM for more urgent tasks, preventing system slowdown or crashes.